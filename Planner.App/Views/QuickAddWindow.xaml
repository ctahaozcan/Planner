<Window x:Class="Planner.App.Views.QuickAddWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hızlı ekle" Height="180" Width="520"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Topmost="True" Background="{DynamicResource AppBgBrush}">
    <StackPanel Margin="16">
        <TextBlock Text="Hızlı ekle" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,8"/>
        <TextBox x:Name="InputBox" Height="44" FontSize="15"
                 Text="{Binding QuickAddText, UpdateSourceTrigger=PropertyChanged}">
            <TextBox.InputBindings>
                <KeyBinding Key="Return" Command="{Binding QuickAddCommand}"/>
            </TextBox.InputBindings>
        </TextBox>
        <TextBlock Text="{Binding QuickAddPreview}" Style="{StaticResource MutedText}" Margin="0,8,0,0"/>
    </StackPanel>
</Window>
